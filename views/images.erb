<!DOCTYPE html>
<!DOCTYPE html>
<html>
   <head>
       <meta charset="utf-8">
      <meta content='width=auto, initial-scale=1.0, maximum-scale=1.0' name='viewport'/>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
      <style>



      </style>
   </head>
   <body style="margin:0">
        <div id="container">
        <% images.each do |key, value| %>
            <div class="col-xs-12">
              <h3><%=key%></h3>
            </div>

              <div class="col-xs-12">
                <%value.each do |val|%>
                <div class="col-xs-4">
                  <div class="thumbnail">
                    <img src='<%=url + val.key%>' alt="..." style="height:100%;max-height:400px;">
                    <div class="caption">
                      <h3><%=val.key.sub('images/','')%></h3>
                      <p><%=val.last_modified.utc.strftime("%I:%M%p UTC")%></p>
                      <p><a href="<%=url + val.key%>" target="_blank" class="btn btn-primary" role="button">Original</a>
                      <a href="<%=url + val.key.sub('images','thumbnails')%>"  target="_blank" class="btn btn-default" role="button">Thumbnail</a></p>
                    </div>
                  </div>
                </div>
                <%end%>
            </div>

        <%end%>
        </div>
   </body>
</html>
